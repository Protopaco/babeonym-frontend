# Align drawer action button sizing

Make the drawer action buttons visually consistent.

Current issue:
- `Set Filters` is compact.
- `View Full Name List` is full-width.
- The mismatch is noticeable now that the drawer styling is more polished.

Notes:
- Decide the intended shared drawer action button sizing/pattern before changing.
- Keep styles theme-token based.
- Preserve existing button behavior.
