# Share Themed Scrollbar Styling

## Context

The Decades list has a themed scrollbar treatment, but the drawer scrollbar does not match it.

## Future Goal

Make scrollbar styling consistent across scrollable drawer/filter components.

## Notes

- The drawer scrollbar should match the Decades scrollbar treatment.
- Investigate whether scrollbar CSS should become a shared setting or utility that can be applied to different components.
- Avoid duplicating scrollbar rules if a shared pattern is practical.
- Use theme variables only.
