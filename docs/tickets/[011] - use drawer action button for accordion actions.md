# Use drawer action button for accordion actions

There are additional drawer-style action buttons inside the filter accordions.

Update those controls to use the shared `DrawerActionButton` component where appropriate.

Notes:
- Use the current drawer accordion UI screenshot as reference.
- Preserve current button behavior.
- Keep drawer button styling centralized.
- If the existing accordion actions need a different size variant, discuss before adding a new pattern.
