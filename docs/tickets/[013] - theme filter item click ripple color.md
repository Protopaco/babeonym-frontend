# Theme filter item click ripple color

Filter accordion list items show a dark grey click/ripple shading when selected or clicked.

Replace that interaction color with an on-theme color.

Notes:
- Likely target MUI `TouchRipple`, active state, or selected state styling for `FilterListItem`.
- Use theme variables only.
- Preserve the existing hover highlight.
- Check both desktop drawer and mobile filter section.
