import Typography from '@mui/material/Typography';
import '@/components/Header/AccountLink/AccountLink.css';

export default () => {
  return (
    <Typography variant="body1" color="primary" id="account-link">
      Account
    </Typography>
  );
};
