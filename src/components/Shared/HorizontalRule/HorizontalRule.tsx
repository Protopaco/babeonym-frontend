import './HorizontalRule.css';

// The one line the app draws to separate content. Edges of bars are not this —
// they belong to the bar they bound.
const HorizontalRule = () => <hr className="horizontal-rule" />;

export default HorizontalRule;
