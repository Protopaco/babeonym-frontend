// Names this long or longer are set a step smaller in a name chip. Chips are a
// fixed width and a name is never cut short, so the longest names shrink to fit
// instead. 12 is roughly where a name in League Spartan at the large chip size
// stops fitting beside the chip's marker; below it every name fits at full size.
export const LONG_NAME_LENGTH = 12;
